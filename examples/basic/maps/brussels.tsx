import { Brussels } from "../src/brussels.tsx";

import type { ReactNode } from "react";

/** The `Light` style at a wide crop. See `src/brussels.tsx`. */
const BrusselsPreview = (): ReactNode => <Brussels />;

export default BrusselsPreview;
