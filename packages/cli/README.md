# stillmap

CLI for [stillmap](https://github.com/abinnovision/stillmap).

**Not yet implemented.** This package reserves the name. The planned
`stillmap dev` preview server watches a directory of map components and
re-renders them on save.

For the library, install [`@stillmap/react`](../react).

## Licence

MIT
